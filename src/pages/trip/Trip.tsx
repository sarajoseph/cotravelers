import { WebContainer } from '../WebContainer'
import { Text } from '@chakra-ui/react'

export const Trip = () => {
  return (
    <WebContainer>
      <Text>Trip page</Text>
    </WebContainer>
  )
}